# Epic 00: Foundation

- [ ] Initialize monorepo workspace (`apps/web`, `apps/api`, `packages/`).
- [ ] Set up base `package.json` files for root and apps.
- [ ] Configure `apps/web` with React + Vite + MUI base template.
- [ ] Configure `apps/api` with Node.js + Express (ES Modules).
- [ ] Set up shared `packages/` for Zod schemas.
- [ ] Configure Supabase project and database connection.
- [ ] Create initial database migration script.
